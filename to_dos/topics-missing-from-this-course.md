# Topics missing from this course

Compared with ScholarNest *Mastering Azure Databricks* (code notebooks only; their `*docs*` folders were not used). These topics are not on `COURSE_MODULES.md` and are not taught in any learner notebook.

- Structured Streaming
- Auto Loader
- `COPY INTO`
- Delta Live Tables / streaming pipelines
- `APPLY CHANGES` / SCD Type 2 in DLT
- ZORDER
- Column mapping / `DROP COLUMN` rewrite
- `CONVERT TO DELTA`
- Change Data Feed
- Liquid clustering
- Predictive Optimization (named in Module 10, not taught)
- Shallow / deep `CLONE`
- `REPLACE WHERE` / `INSERT REPLACE`
- Identity / generated columns
- Table `CHECK` / `NOT NULL` constraints
- UniForm / Iceberg
- Delta Sharing
- UC row filters / column masks / ABAC
- Lakehouse Federation
- SQL warehouses / Databricks SQL dashboards
- Secret scopes
- Cluster policies / instance pools
- SCIM / account-console metastore setup
- Azure Access Connector walkthrough
- DBFS mounts / FileStore
- `%scala` / `%sh` magics as a lesson
- `dbutils.widgets` dropdowns as a lesson
- Jobs API 2.1 from notebooks
- Azure DevOps build / release pipelines
- Streaming lakehouse capstone (gym / IoT)
- Hive-style `CREATE DATABASE … LOCATION` gold DB
- Databricks sample datasets (`sf-fire-calls`) as teaching data
- Machine learning (MLflow, Feature Store, model serving)
- General Azure admin (VNets, ADF, Synapse, Event Hubs)
