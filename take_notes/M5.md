# M5 — Reading, Writing, and Schemas

Created: 2026-07-30 11:34 IST

Living handoff note for Module 5. Append new sections as work continues.
In a new chat: `@take_notes/M5.md` plus the relevant notebook file(s).

## Preferences

- Do not add `# noqa` / `# pyright: ignore` for Databricks builtins (`display`,
  `dbutils`, `spark`). Local “not defined” warnings are expected — leave code
  as-is.

## Handoff — 2026-07-30

### Done

- `04 - Reading Parquet.py`: section **5c. Schema mismatch**
  - Compatible cast (`hour_of_day` int → bigint)
  - Date declared as int → epoch day counts (e.g. 20522), not nulls / not
    dropped rows
  - Extra schema column → nulls
  - Omitted file column → dropped from DataFrame
- Framing, objectives table, and summary updated to match runtime behavior

### Watch / next

- Re-run Parquet 5c in Azure Databricks if not already validated
- Avro notebook still has an orphan “schema disagrees” note (no demo yet) —
  optional follow-up
- Author runtime validation evidence stays author-owned (`docs/validation/`)

### Key files

- `05 - Reading, Writing, and Schemas/04 - Reading Parquet.py`
- `05 - Reading, Writing, and Schemas/06 - Reading Avro.py`
- `docs/data/dataset-overview.md`
